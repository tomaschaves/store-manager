// const checkFields = (validIds, sales) => {
//     if (hasProductID) {
//       console.log({ type: 400, message: '"productId" is required' });
//       return { type: 400, message: '"productId" is required' };
//     }
// };

// module.exports = { checkFields };
